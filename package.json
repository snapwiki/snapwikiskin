{
  "private": true,
  "scripts": {
    "test": "prettier --check .",
    "fix": "prettier --write .",
    "lint": "stylelint \"**/**.css\" --fix",
    "minify": "minhtml --collapse-whitespace --remove-comments --input-dir ./templates --output-dir ./templates/minified --file-ext mustache"
  },
  "devDependencies": {
    "browserslist": "^4.20.2",
    "browserslist-config-wikimedia": "^0.3.0",
    "minhtml": "^1.0.1",
    "prettier": "^2.6.2",
    "stylelint": "^14.8.3",
    "stylelint-config-prettier": "^9.0.3",
    "stylelint-config-wikimedia": "^0.12.2",
    "stylelint-no-unsupported-browser-features": "^5.0.3"
  }
}
